version: '3.8'

services:
  bpost-predictor:
    build: .
    container_name: bpost-predictor
    ports:
      - "${PORT:-5000}:${PORT:-5000}"
    environment:
      - PORT=${PORT:-5000}
    volumes:
      - ./config.json:/app/config.json
      - ./app.log:/app/app.log
    restart: unless-stopped
